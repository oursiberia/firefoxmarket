/**
 * A Factory for accessing the Mozilla marketplace
 */
app.factory("Market",['$http',function($http){
    var base_url = "https://marketplace.firefox.com/api/v1/apps/app/twitter/";      // append ?format=JSON if you want to test url in browser
    var API = {

    };


    return API;

}]);