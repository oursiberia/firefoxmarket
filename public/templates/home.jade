section#HOME(ng-controller="home",class="section ajax")
        div#search-home
            div.wrap
                input#homesearch(type="text",name="search",ng-model="searchbox",placeholder="Search",ng-change="searchApp()")
        div#search-home-result
            div.wrap
                div.searchresult(ng-repeat="result in results")
                    a(ng-href="/app/{{result.id}}")
                        img(ng-src="{{result.icon}}")
                    h2.result-title {{result.name}}
                    p.result-author {{result.author}}


        div#featured
            div.wrap
                h1 Featured Apps
                div.apps(class="home-apps")
                    div(class="{{app.classname}} featured {{app.showfirst}} with-description",ng-repeat="app in featured_apps")

                        div.app-wrap
                            div.app-details
                                div.app-image
                                    a(ng-href="/app/{{app.id}}")
                                        img.app-icon(ng-src="{{app.icon}}")

                                div.app-copy
                                    div.app-download-button
                                        p Free
                                    a(ng-href="/app/{{app.id}}")
                                        h2.app-title {{app.name}}
                                        p.app-author {{app.author}}
                                        div(starrating,data-rating="{{app.rating}}")


                            div.app-description
                                p(ng-bind-html="app.description | htmlRender ")


        div#ecollections(class="home-collections")
            div.wrap
                div.collections
                    div.collection
                        div.layerbackground
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app

                        div.collection-copy
                            h2 Our Staff Picks
                            p fiehsoifheoisfhoesifheosifhoeishfoeishfo

                    div.collection
                        div.layerbackground
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app
                        div.collection-copy
                            h2 Top three Games
                            p fiehsoifheoisfhoesifheosifhoeishfoeishfo
                    div.collection
                        div.layerbackground
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app
                        div.collection-copy
                            h2 Great Apps for the workplace
                            p fiehsoifheoisfhoesifheosifhoeishfoeishfo


        div#category-list
            div.wrap
                div.category(ng-repeat="item in appcategories")
                    div.category-title(data-name="{{item.name}}")
                        div.title-wrap
                            div.icon
                            div.title-copy
                                h3 {{item.name}}


                    div(categoryapps,data-categoryname="{{item.slug}}")
