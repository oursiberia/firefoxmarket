section#HOME(data-ng-controller="home",class="section ajax")
        div#search-home
            div.wrap
                input#homesearch(type="text",name="search",data-ng-model="searchbox",placeholder="Search for anything...",data-ng-change="searchApp()")
        div#search-home-result
            div.wrap
                div.searchresult(data-ng-repeat="result in results")
                    a(data-ng-href="/app/{{result.id}}")
                        img(data-ng-src="{{result.icon}}")
                    h2.result-title {{result.name}}
                    p.result-author {{result.author}}


        div#featured
            div.wrap
                h1 Featured Apps
                div(class="home-apps")
                    div(class="{{app.classname}} featured {{app.showfirst}} with-description",data-ng-repeat="app in featured_apps")

                        div.app-wrap
                            div.app-details
                                div.app-image
                                    a(data-ng-href="/app/{{app.id}}")
                                        img.app-icon(data-ng-src="{{app.icon}}")

                                div.app-copy
                                    div.app-download-button
                                        p Free
                                    a(data-ng-href="/app/{{app.id}}")
                                        h1.app-title {{app.name}}
                                        h2.app-author {{app.author}}
                                        div(starrating,data-rating="{{app.rating}}")


                            div.app-description
                                p(data-ng-bind-html="app.description | htmlRender ")


        div#ecollections(class="editorial-collections")
            div.wrap
                div.collections
                    div.collection
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app

                        div.collection-copy
                            h1 Our Staff Picks
                            p Each week we compile the five most popular apps according to you—the Mozilla community—and list them here.

                    div.collection
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app
                        div.collection-copy
                            h1 Top three Games
                            p Each week we compile the five most popular apps according to you—the Mozilla community—and list them here.
                    div.collection
                        div.collection-apps
                            div.collection-app
                            div.collection-app
                            div.collection-app
                        div.collection-copy
                            h1 Great Apps for the workplace
                            p Each week we compile the five most popular apps according to you—the Mozilla community—and list them here.


        div#category-list
            div.wrap
                div 
                    h1 Most Popular
                    h1 Recently Released
                    button View All Categories
                div.category(data-ng-repeat="item in appcategories")
                    div.category-title(data-name="{{item.name}}")
                        div.title-wrap
                            div.icon
                            div.title-copy
                                h3 {{item.name}}


                    div(categoryapps,data-categoryname="{{item.slug}}")
