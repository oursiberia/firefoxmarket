//abuse starts out in a "closed" state.
div#feedback(class="modal closed")
    div.modalBox
        h2(translate) Send Feedback
        div.closeModal
        textarea(name="review",placeholder="enter your feedback")
        button(data-ng-click="submitFeedback()",translate) Send
